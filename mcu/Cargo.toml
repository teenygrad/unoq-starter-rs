[package]
name = "mcu"
version = "0.1.0"
edition = "2024"
description = "Hello world for STM32U585 MCU (Arm Cortex-M33)"

[dependencies]
cortex-m = { version = "0.7", features = ["critical-section-single-core"] }
cortex-m-rt = "0.7"
cortex-m-semihosting = "0.5"
panic-halt = "1.0"
